---

namespace: 'oxlorg'
name: 'opnsense'
version: 26.1.11
readme: 'README.md'
authors:
  - 'OXL IT Services <contact+opnsense@OXL.at>'
description: "Ansible Collection to manage OPNsense firewalls using its API"
license_file: 'LICENSE.txt'
tags:
  - 'firewall'
  - 'api'
  - 'opnsense'
  - 'iac'
  - 'network'
  - 'security'
  - 'filter'
dependencies: {}
repository: 'https://github.com/O-X-L/ansible-opnsense'
documentation: 'https://ansible-opnsense.oxl.app'
homepage: 'https://www.OXL.at'
issues: 'https://github.com/O-X-L/ansible-opnsense/issues'
build_ignore: [
  '*_pytest.py', '*_testdata.py', '*pytest_cache', '*/_tmpl.py',
  '.**', 'pytest.ini', 'requirements_*.txt',
  'docs**', 'scripts**', 'venv**', 'Makefile', 'legacy**',
  # Self-referential symlink chain left behind by running scripts/unit_test.sh directly instead
  # of through its designed invocation -- harmless where it sits, but breaks
  # `ansible-galaxy collection build` (a symlink inside it points outside the tarball, which
  # tar can't resolve on extract). Excluding it from the package rather than deleting it.
  'collections**',
]
